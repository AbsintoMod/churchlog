//captura event do language
   var select = document.getElementById('countries');
	  $('#countries').on('change',()=>{
      var value = select.options[select.selectedIndex].value;
	    console.log(value)
    })

    $('#countries').on('focus',e=>{
      console.log(e)
    })
    $('#countries').on('blur',e=>{
      console.log(e)
    })

    $('#countries[option:selected]').on('click',e=>{
      if (select.options[select.selectedIndex]) {
        console.log(e)  
      }
    })

    
    $('#countries').on({
      change:()=>{console.log(event.type +': ' +event.which)},
    })





    SELECT
	select_city.id, select_city.city, select_state.id, select_state.state, select_country.id, select_country.country
FROM select_city
INNER JOIN
    select_state ON select_state.id = select_city.id_select_state
INNER JOIN
    select_country ON select_country.id = select_state.id_select_country


              /*const xhttp = new XMLHttpRequest();
          xhttp.onload = function() {
            if (this.readyState == 4 && this.status == 200) {
              console.log(xhttp.responseText);
              console.log(xhttp)
            }else{
              alert('deu errado');
            }
          }
          xhttp.open("POST", "../../../../php/reg_departamento.php");
          xhttp.send();*/